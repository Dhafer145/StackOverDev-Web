{% extends 'baseback.html.twig' %}

{% block title %}Bilans des etudiants{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="card shadow">
            <div class="card-header py-3">
                <p class="text-primary m-0 font-weight-bold">Liste des etudiants </p>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-6">
                        <div class="text-md-right dataTables_filter" id="dataTable_filter"><label>
                                <input type="search" class="form-control form-control-sm" aria-controls="dataTable"
                                       placeholder="Search"></label>
                        </div>
                    </div>
                </div>
                <div class="table-responsive table mt-2" id="dataTable" role="grid" aria-describedby="dataTable_info">
                    <table class="table my-0" id="dataTable">
                        <thead>
                        <tr>
                            <th>Etudiant</th>
                            <th>Debut de Stage</th>
                            <th>Milieu de Stage</th>
                            <th>Fin de Stage</th>
                        </tr>
                        </thead>
                        {% set rep = false %}
                        {% set rep1 = false %}
                        {% set rep2 = false %}




                        {% for user in users %}

                        {% for dem in user.etudReps %}


                        {% set rep = dem.IndexPeriode %}





                        {% endfor %}



                            <tbody>
                        <tr>
                            <td>{{ user.getUserName }}</td>
                            {% if user.etudReps|length > 0 and rep == 1 %}

                            <td><a href="{{ path('visualisation_bilan_show',{id : user.id}) }}" class="badge badge-success">Rempli</a></td>

                            {% else %}
                                <td><span class="badge badge-danger">Non Rempli</span></td>
                            {% endif %}

                            {% if user.etudReps|length > 0 and rep == 2  %}
                                <td><a href="{{ path('visualisation_bilan_show_middle',{id : user.id}) }}" class="badge badge-success">Rempli</a></td>
                            {% else %}
                                <td><span class="badge badge-danger">Non Rempli</span></td>
                            {% endif %}

                            {% if user.etudReps|length > 0 and rep == 3 %}
                            <td><a href="{{ path('visualisation_bilan_show_end',{id : user.id}) }}" class="badge badge-success">Rempli </a></td>
                            {% else %}
                            <td><span class="badge badge-danger">Non Rempli </span></td>
                            {% endif %}
                        </tr>
                        </tbody>
                        {% endfor %}
                    </table>
                </div>
                <div class="row">

                    <div class="col-md-6">

                    </div>
                </div>
            </div>
        </div>
    </div>

        {% for user in users %}

        {% endfor %}

{% endblock %}
