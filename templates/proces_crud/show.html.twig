{% extends 'base.html.twig' %}

{% block title %}ProcesVerbal{% endblock %}

{% block body %}
    <h1>Proces Verbal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ proces_verbal.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ proces_verbal.date ? proces_verbal.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Membre_reunion</th>
                <td>{{ proces_verbal.membreReunion }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ proces_verbal.Description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('proces_c_r_u_d_index') }}"> Back to list </a>

    <a href="{{ path('proces_c_r_u_d_edit', {'id': proces_verbal.id}) }}"> Edit </a>

    {{ include('proces_crud/_delete_form.html.twig') }}


{% endblock %}
