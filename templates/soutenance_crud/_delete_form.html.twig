<form method="post" action="{{ path('soutenance_crud_delete', {'id': soutenance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ soutenance.id) }}">
    <button class="btn">Delete</button>
</form>
