<form method="post" action="{{ path('journal_projet_delete', {'id': journal_projet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ journal_projet.id) }}">
    <button class="btn">Delete</button>
</form>
