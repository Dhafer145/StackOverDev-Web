{% extends 'base.html.twig' %}

{% block title %}Journal de Projet{% endblock %}

{% block body %}
    <h1>Journal de Projet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ journal_projet.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ journal_projet.date ? journal_projet.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Tache</th>
                <td>{{ journal_projet.tache }}</td>
            </tr>
            <tr>
                <th>Avis</th>
                <td>{{ journal_projet.avis }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('journal_projet_index') }}">back to list</a>

    <a href="{{ path('journal_projet_edit', {'id': journal_projet.id}) }}">edit</a>

    {{ include('journal_projet/_delete_form.html.twig') }}
{% endblock %}
