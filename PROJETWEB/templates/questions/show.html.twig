{% extends 'baseback.html.twig' %}

{% block title %}Questions{% endblock %}

{% block body %}

    <div class="text-right"><button type="button" class="btn btn-primary"><a href="{{ path('questions_index') }}" class="text-white">retour a la liste</a></button></div>
    <h1>Questions</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ question.id }}</td>
        </tr>
        <tr>
            <th>Question</th>
            <td>{{ question.quest }}</td>
        </tr>
        <tr>
            <th>IndexPeriode</th>
            <td>{{ question.IndexPeriode }}</td>
        </tr>
        <tr>
            <th></th>
            <td><button type="button" class="btn btn-success"> <a href="{{ path('questions_edit', {'id': question.id}) }}" class="text-white">Modifier</a></button>

            </td>
        </tr>
        </tbody>
    </table> <br>

    <div class="text-center">{{ include('questions/_delete_form.html.twig') }}</div> <br>



{% endblock %}
