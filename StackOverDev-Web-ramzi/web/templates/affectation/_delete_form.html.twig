<form method="post" action="{{ path('affectation_delete', {'id': affectation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ affectation.id) }}">
    <button class="btn">Delete</button>
</form>
